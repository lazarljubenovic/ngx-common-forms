import { FormControlNameDirective } from './form-control-name.directive';

describe('FormControlNameDirective', () => {
  it('should create an instance', () => {
    const directive = new FormControlNameDirective();
    expect(directive).toBeTruthy();
  });
});
