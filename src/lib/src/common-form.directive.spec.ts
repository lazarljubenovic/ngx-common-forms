import { CommonFormDirective } from './common-form.directive';

describe('CommonFormDirective', () => {
  it('should create an instance', () => {
    const directive = new CommonFormDirective();
    expect(directive).toBeTruthy();
  });
});
